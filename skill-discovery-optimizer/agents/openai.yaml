interface:
  display_name: "Skill Discovery Optimizer"
  short_description: "Audit Agent Skills before publishing and improve discovery"
  default_prompt: "Use $skill-discovery-optimizer to audit and prepare these Agent Skills before publishing, then verify remote installation and discovery."
policy:
  allow_implicit_invocation: true
